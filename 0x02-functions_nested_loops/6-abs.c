#include "main.h"

/**
 * _abs - prints absolute value of integer
 * @t: The interger to be computed
 * Return: The absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
